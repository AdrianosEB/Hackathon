A live call can make the demo memorable, but it must remain a disclosed, human-approved call to a consenting teammate acting as the billing office. It must never call a real provider or use real claim data.

The call would be an optional final step:

```text
Analyst approves request → agent calls DEMO_BILLING_CONTACT
→ says “This is a hackathon demo” → requests one missing synthetic artifact
→ saves transcript → finding becomes clear or escalate
```

It needs a prerecorded/transcript-fixture fallback, so telephony failure cannot break the demo.

### D9 — Spend the optional-build window on what?

Reply with **A, B, or C**.

ELI10: After the hour-10 core-flow gate, there is room for one ambitious add-on, not two. A claims queue proves scale; a live clarification call proves the closed loop. Trying to rush both risks a fragile demo.

Recommendation: **A** because the live call is more distinctive for judges and completes the story from detection to resolution.

A) **Live clarification call first** *(recommended)*. Note: options differ in kind, not coverage — no completeness score. Use the optional four-hour window for the consented, disclosed teammate call and transcript fallback. Add the Batch Sentinel queue only if the call is working early.

B) **Batch Sentinel queue first.** Note: options differ in kind, not coverage — no completeness score. Use the window for the 25-claim exposure dashboard; attempt the live call only if that is already polished.

C) **Neither; polish the core ledger.** Note: options differ in kind, not coverage — no completeness score. Preserve maximum reliability and rehearse the strongest evidence-ledger walkthrough.